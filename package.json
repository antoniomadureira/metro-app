{
  "name": "metro-app",
  "version": "1.0.0",
  "description": "Metro Porto Schedule App",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --dist-dir dist --public-url ./"
  },
  "dependencies": {
    "pdfjs-dist": "^3.11.174"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}